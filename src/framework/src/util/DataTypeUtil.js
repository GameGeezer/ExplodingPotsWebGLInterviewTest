(function(window) {

    var DataTypeUtil = function( ) {

    };

    DataTypeUtil.FLOAT_SIZE_BYTES = 4;

    DataTypeUtil.INTEGER_SIZE_BYTES = 4;

    DataTypeUtil.SHORT_SIZE_BYTES = 2;

    DataTypeUtil.UNSIGNED_SHORT_MAX = 65635;

    window.DataTypeUtil = DataTypeUtil;

})(window);